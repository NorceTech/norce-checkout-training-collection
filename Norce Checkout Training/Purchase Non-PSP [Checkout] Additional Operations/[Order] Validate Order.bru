meta {
  name: [Order] Validate Order
  type: http
  seq: 10
}

post {
  url: {{orderUrl}}/api/v1/checkout/orders/{{orderId}}/validate
  body: none
  auth: inherit
}

headers {
  x-merchant: {{merchant}}
  x-channel: {{channel}}
}

tests {
  
  
}

docs {
  # Validate the order
  
  This will run all registered validations on the order
}
